const testVar = {}

function testFunc() {
    return "hi"
}

let superbowlWin = (record) => {
    let result = record.find(record => record.result === "W")
    return
    if (result === "W") {
        result.year
    } else {
        undefined
    }
}